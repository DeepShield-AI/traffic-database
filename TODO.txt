DiskAgent并行支持——不需要，一个DiskManager->多个DiskAgent，且保证写入位置唯一
数据包处理线程竞争磁盘块——完成
DiskManager修改：从DataBlock接收由数据包处理线程计算的数据块编号；磁盘需要按RSS队列交错分区(不需要！只要通过位置竞争保证每个数据块位置唯一！)——完成
用于判断DiskAgent是否完成任务实现异步停止的接口——完成
MemoryManager、DiskManager重新接入，使用DataBlockBuffer——完成
    DataBlock设计保证
        (i)位于不同磁盘块的数据不会写入同一缓冲数据块中（即便对应相同缓冲数据块）
        (ii)所有RSS线程写入区域可以被连续的total_block_num个数据块覆盖，且磁盘数据块数量至少为total_block_num的数（4？）倍
        (iii)若数据块b未写入磁盘，数据块a写入缓冲区的时间在b之后，一定满足a>b或a>b+total_block_num（a,b均为磁盘数据块编号）
            保险起见，使用2*total_block_num作为分界
    
BitMap实现，接入DiskManager（数据包处理时依据RSS分区编号：数据包处理线程为索引写入RSS唯一的编号；并发：通过字节拆分实现无锁并行写入）——完成，待测试
    注意（难点）：一级索引（位图）的更新发生在数据块、磁盘元数据覆盖之前
    覆盖难点相关设计：预留位图列，更新位图时不直接原本的数据块对应列，而是覆盖备用列，而位图清理线程在更新磁盘元数据后清除原本列
    并行设计：通过将字节位拆分到不相邻列，写入避免同时写入同一字节；设置列写入边界（cleaning-2*backup,cleaning）进一步保证写入安全，并可以区分覆盖后的列
        Insight：磁盘块大致（在较粗粒度上）按时间顺序更新
            数据包处理线程同时处理的数据块一定连续，因此同时被数据包处理的数据块位置差值不超过RSS队列个数
        注意：限制备用列个数不超过总列数八分之一，保证不会有同字节被写入
    索引建立线程在可写入索引块中找到对应位图列并插入
    索引持久化线程从写满的索引块block中找到对应位图列col：(i)修改索引块位置为不可写入；(ii)将位图列位置写入持久化的磁盘数据块；(iii)将col+backup列清理并放入block+backup的索引块位置
    
Bloom filter实现(完成)

跳表修改，数据块倒序排序（后置节点批量回收）&索引线程对应的内存池——除节点读出外完成（修改）
    Insight：
        同一索引建立线程接收的同一RSS队列的索引键值对的时间不会乱序
        跳表查询和插入时只会受到目标节点前置节点的影响
与数据块绑定的跳表（完成）

与数据包处理线程绑定的内存池（完成）

索引缓冲区(完成)
    索引插入过程与检查过程

索引持久化线程（完成）
    所有索引持久化线程共享的跳表维护环，竞争检查跳表（11～15位为空位，直接进行下一个跳表的检查），跳表节点超过限制则断链读出到缓冲区
        跳表检查方式？磁盘写入位置边界值？
    索引持久化线程共享、与索引类别对应的索引块缓冲区，通过与数据块缓冲区相同的结构写入缓冲区对应位置
        不需要与索引类别对应？
与数据落盘相同的索引落盘线程

待分配的DataBlock回收（完成）

磁盘块回收及回收时索引覆盖逻辑（完成，由索引持久化线程+disk_buffer实现）

索引落盘与索引块缓冲区回收（完成）
    DiskAgent复用
    回收线程复用
    落盘线程复用

控制器与组件组装（完成）

整体测试

当前mmap方法并非分配大页内存

skiplist改回最初版本
    阶段一：DPDKReader提供索引键值，IndexGenerator创建node并排序，IndexPersister回收（完成并测试，4捕获线程13Gbps，8捕获线程20Gbps）
    阶段二：DPDKReader提供聚合后首个数据包位置，IndexGenerator解析并创建node并排序，IndexPersister回收（不必要，瓶颈不在索引键值生成与复制，而是传递过程）
    阶段三：DPDKReader提供索引键值，IndexGenerator组建小顶堆，IndexPersister排序并回收

DPDKReader提供索引键值，IndexGenerator组建小顶堆，IndexPersister排序并回收
完善queier
FloSIS流缓存方式实现
不必要：Trie树等索引方式实现

固定时间点/固定大小的链表截断——磁盘回收大小/时间范围与截断大小应该相同（与位图索引的检索失效区域也相同？），如一次回收1K个数据块（1GB），而非一个一个数据块回收?（不需要）
    或：大数据块+块间前向链表？